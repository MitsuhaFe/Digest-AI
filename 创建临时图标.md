# 创建临时图标指南

由于项目需要图标文件才能正常加载，以下提供几种快速创建临时图标的方法：

## 方法 1: 使用 Emoji 截图（最简单）

1. 打开任何文本编辑器或浏览器
2. 输入 emoji：📚 （书本 emoji）
3. 放大字体到合适大小
4. 截图并裁剪为正方形
5. 调整大小为 16x16、48x48、128x128 像素
6. 保存为 PNG 格式到 `icons/` 目录

## 方法 2: 使用在线工具

### Favicon Generator
1. 访问 https://www.favicon-generator.org/
2. 上传任何图片或使用文字生成
3. 下载生成的图标包
4. 重命名为 icon16.png, icon48.png, icon128.png

### Canva
1. 访问 https://www.canva.com/
2. 选择"自定义尺寸"
3. 创建 128x128 的设计
4. 添加文字"AI"或相关图形
5. 导出为 PNG
6. 使用图片编辑器调整为不同尺寸

## 方法 3: 使用命令行（需要 ImageMagick）

如果您安装了 ImageMagick：

```bash
# 创建简单的文字图标
convert -size 128x128 xc:white -font Arial -pointsize 72 -fill "#667eea" -gravity center -annotate +0+0 "AI" icons/icon128.png
convert icons/icon128.png -resize 48x48 icons/icon48.png
convert icons/icon128.png -resize 16x16 icons/icon16.png
```

## 方法 4: 使用 Node.js 脚本

创建并运行以下脚本（需要先安装 canvas: `npm install canvas`）:

```javascript
const { createCanvas } = require('canvas');
const fs = require('fs');

function createIcon(size, filename) {
  const canvas = createCanvas(size, size);
  const ctx = canvas.getContext('2d');
  
  // 背景渐变
  const gradient = ctx.createLinearGradient(0, 0, size, size);
  gradient.addColorStop(0, '#667eea');
  gradient.addColorStop(1, '#764ba2');
  ctx.fillStyle = gradient;
  ctx.fillRect(0, 0, size, size);
  
  // 文字
  ctx.fillStyle = 'white';
  ctx.font = `bold ${size * 0.4}px Arial`;
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText('AI', size / 2, size / 2);
  
  // 保存
  const buffer = canvas.toBuffer('image/png');
  fs.writeFileSync(filename, buffer);
}

createIcon(128, 'icons/icon128.png');
createIcon(48, 'icons/icon48.png');
createIcon(16, 'icons/icon16.png');

console.log('图标创建完成！');
```

## 方法 5: 下载免费图标

访问以下网站下载免费图标：
- https://www.flaticon.com/ (搜索 "book" 或 "reading")
- https://icons8.com/ (搜索 "library" 或 "AI")
- https://www.iconfinder.com/

## 临时解决方案

如果您只是想快速测试功能，可以使用任何 PNG 图片：

```bash
# Windows (使用 PowerShell)
# 复制同一个文件三次
copy 任何图片.png icons\icon16.png
copy 任何图片.png icons\icon48.png
copy 任何图片.png icons\icon128.png
```

Chrome 会自动缩放图片，虽然可能不够清晰，但足够测试使用。

---

**建议**: 在正式发布前，请设计专业的图标，确保在不同尺寸下都清晰美观。

